# 迭代复盘（最近几轮）

## 经验总结

1. 先保证可回归：每轮改动后必须立即跑 `lint + build`，避免问题累积到最后集中爆发。
2. 避免隐式副作用：评分逻辑里不应原地修改 React state 内对象，导出结果应使用快照对象。
3. 优先小步重构：样式抽离应先抽公共块（容器、按钮、卡片），再逐步替换复杂组件。
4. 编码要有硬约束：中文项目在 Windows 环境下，必须统一 UTF-8 与换行规则，降低乱码和 diff 噪音。
5. 安全债要及时清：发现 `npm audit` 高危项后，优先做最小传递依赖升级并回归验证。

## 已落地迭代

- 增加 `.editorconfig`，统一编码、换行和缩进规则。
- 增加统一检查命令 `npm run check`（`lint + build`）。
- 完成核心组件可维护性优化（样式抽离、评分逻辑重构、CSV/存储稳健性修复）。

## 后续建议

1. 引入测试框架（Vitest）并补充评分函数与 CSV 导出的单元测试。
2. 将剩余文本资源从代码中提取到集中常量文件，降低文案修改成本。
3. 在 CI 中接入 `npm run check`，把本地规范提升为仓库门禁。
